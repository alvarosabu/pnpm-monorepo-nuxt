{
  "name": "@pnpm-monorepo-nuxt/blog",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:ci": "pnpm run -w build:ui && nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.3"
  },
  "dependencies": {
    "@pnpm-monorepo-nuxt/ui": "workspace:^0.0.0"
  }
}
